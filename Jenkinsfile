pipeline {
    agent any

    stages {
        stage('SCM checkout') {
            steps {
                echo 'checkout completed'
            }
        }
        stage('Build') {
            steps {
                echo 'Build completed'
            }
        }
        stage('Test') {
            steps {
                echo 'Test completed'
              //  sh 'date'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy completed'
            }
        }
    }
}
